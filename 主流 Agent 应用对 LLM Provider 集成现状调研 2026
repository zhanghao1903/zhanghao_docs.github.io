# 主流 Agent 应用对 LLM Provider 集成现状调研（2026）

> 日期：2026-05-22  
> 主题：主流 Agent 应用、Agent Builder、AI Coding Agent、模型网关对 LLM Provider 的集成现状，以及文件/RAG/工具层的行业演进方向。

---

# 1. 行业整体做到什么地步了

截至 2026 年，主流 Agent 应用对 LLM Provider 的集成已经普遍做到：

```text
多 Provider
BYOK（Bring Your Own Key）
OpenAI-compatible
Streaming
Tool Calling
文件上传
RAG / 知识库
Workflow / Agent
本地模型
```

但大多数产品：

```text
还没有真正逐家打穿 Provider 原生文件 API
没有统一不同 Provider 的文件能力
没有统一 citation / 页码 / 引用格式
没有统一 usage / quota / cleanup
没有真正做好 capability abstraction
```

行业普遍停留在：

```text
广覆盖
```

而不是：

```text
深集成
```

---

# 2. LLM Provider 集成成熟度分层

建议把行业集成成熟度分成 7 层。

| 层级 | 能力 | 行业现状 |
|---|---|---|
| L1 | API Key / Base URL / Model Name 配置 | 基本都做到了 |
| L2 | 多 Provider 预置 | 很普遍 |
| L3 | OpenAI-compatible 兜底 | 非常普遍 |
| L4 | 统一 Chat / Streaming / Tool Calling | 部分成熟 |
| L5 | 自建文件上传 / RAG / 向量库 / 知识库 | 主流产品基本都在做 |
| L6 | Provider 原生文件 API 深度集成 | 不普遍 |
| L7 | 成本 / 路由 / fallback / quota / usage 治理 | 多交给 LiteLLM / OpenRouter |

一句话总结：

> 行业已经做到“多模型可用”，但还没有做到“多模型差异真正被抽象”。

---

# 3. 主流产品的集成路线

行业已经明显分化成几条路线：

| 路线 | 代表产品 | 核心特点 |
|---|---|---|
| Agent Builder | Dify、Coze、Botpress | 平台化、Workflow、Knowledge、插件 |
| 自托管 Chat/RAG | Open WebUI、LibreChat、AnythingLLM、LobeChat | BYOK、自建 RAG、多模型 |
| Low-code Agent/RAG | Langflow、Flowise、n8n | 节点化、工具化、工作流 |
| Coding Agent | Cline、Roo Code、Continue、Cursor、Windsurf | 强工具执行、代码上下文、终端 |
| Model Gateway | LiteLLM、OpenRouter、Vercel AI Gateway | 模型路由、fallback、统一 API |

---

# 4. Dify

Dify 是目前较成熟的平台型方案之一。

其核心能力：

```text
Workspace 级 Provider 管理
BYOK
Knowledge
Workflow
Agent
Plugin
```

它的重点：

```text
不是逐家深度接入 provider
而是把：
Provider → Workspace Resource
File → Knowledge
Tool → Plugin
```

统一抽象成平台资源。

## Dify 的特点

### 优点

```text
Provider 管理成熟
Knowledge/RAG 成熟
Workflow 强
Agent 能力完整
```

### 缺点

```text
Provider 原生文件 API 深度一般
更多走自己的 Knowledge/RAG
```

## 启发

Dify 的真正价值：

> 把 Provider、Knowledge、Workflow 抽象成统一平台资源。

---

# 5. Open WebUI

Open WebUI 的路线非常典型：

```text
协议优先
```

核心思想：

```text
优先围绕：
OpenAI Chat Completions
OpenAI-compatible
```

而不是：

```text
逐家 provider 深度定制
```

## 特点

```text
BYOK
Ollama
OpenAI-compatible
多 Provider
文件上传
Web Search
Tool
Code Execution
```

## 它的策略

```text
Provider 广覆盖
能力浅统一
```

大量能力通过：

```text
LiteLLM
OpenRouter
Middleware
```

完成。

## 启发

这是行业非常典型的一条路线：

> “广覆盖 + OpenAI-compatible + 自建 RAG”。

---

# 6. LibreChat

LibreChat 更像：

```text
开源版 ChatGPT + Agent UI
```

它的特点：

```text
Agents
RAG API
多 Provider
BYOK
```

重点：

```text
文件问答主要通过：
自己的 RAG API
```

而不是：

```text
逐家 Provider 原生文件 API
```

## 启发

说明：

> 文件问答完全可以独立于 Provider 原生文件能力。

---

# 7. AnythingLLM

AnythingLLM 是：

```text
BYOK + 自建 RAG
```

路线的代表。

## 特点

```text
文档上传
向量数据库
Agents
本地模型
Workspace
多 Provider
```

Provider 的作用：

```text
主要负责：
推理
```

而：

```text
文件
Embedding
RAG
Citation
```

主要掌握在产品自己手里。

## 启发

这是非常值得参考的一点：

> 文件和 RAG 层应该属于产品，而不是属于 Provider。

---

# 8. LobeChat / LobeHub

LobeChat 的特点：

```text
Provider 数量极多
```

支持：

```text
OpenAI
Anthropic
Gemini
OpenRouter
DeepSeek
Qwen
Moonshot
智谱
MiniMax
火山
百度
讯飞
等等
```

## 但它的文件层：

```text
依然是：
自建 Knowledge Base
```

使用：

```text
PGVector
Embedding
Object Storage
Unstructured
```

等技术栈。

## 启发

说明：

> 即使 Provider 覆盖做到非常高，文件层依然建议自己掌握。

---

# 9. Langflow

Langflow 是：

```text
Low-code AI Builder
```

重点：

```text
Agent
MCP
Workflow
RAG
节点式编排
```

## 特点

```text
Provider 组件化
File Component
MCP
Workflow
```

## 启发

Langflow 的核心启发：

> 文件、Provider、向量库、MCP 都应该是组件，而不是写死在模型调用里。

---

# 10. Flowise

Flowise 的特点：

```text
视觉化 Agent Workflow
```

大量能力依赖：

```text
LangChain ecosystem
```

## 特点

```text
组件化
Workflow
节点式 Agent
```

## Provider 集成特点

```text
广覆盖
但深度较浅
```

## 启发

说明：

> 组件化可以快速覆盖 Provider，但难以保证深度体验一致性。

---

# 11. n8n

n8n 的重点：

```text
不是模型
而是：
Workflow + Tools + External Systems
```

## 特点

```text
AI Agent
Tool Calling
Human Review
422+ integrations
```

## 文件能力

更多通过：

```text
节点
Workflow
RAG
```

实现。

## 启发

Agent 的价值不只是模型。

更重要的是：

```text
工具
审批
业务系统
Workflow
```

---

# 12. Coze / 扣子

Coze 的特点：

```text
平台化
```

重点：

```text
Knowledge
Plugin
Workflow
Channel
Publishing
```

## 文件能力

知识库能力较强。

但：

```text
Provider 抽象不透明
```

## 启发

适合：

```text
业务用户
低代码
```

但：

```text
不适合做极致开放的 BYOK 平台
```

---

# 13. Botpress

Botpress 更偏：

```text
客服 Agent
企业业务 Agent
```

## 特点

```text
Knowledge Base
Channels
Business Workflow
```

## 文件能力

强调：

```text
documents
websites
structured data
```

## 启发

垂直业务 Agent：

```text
更关注业务流程
而不是 Provider 深度
```

---

# 14. Coding Agent 路线

Coding Agent 和普通 Agent 完全不同。

重点：

```text
代码库
终端
工具执行
文件系统
```

而不是：

```text
PDF / DOCX 文件上传
```

---

# 15. Cline

Cline 的路线：

```text
Every model
No vendor lock-in
```

## 特点

```text
BYOK
终端
Browser
文件系统
Tool execution
Approval
```

## 启发

真正的 Agent：

```text
价值来自：
工具执行闭环
```

而不是：

```text
聊天
```

---

# 16. Roo Code

Roo Code 的特点：

```text
Profile 化
```

不同 profile：

```text
不同 Provider
不同模型
不同参数
```

还能通过：

```text
LiteLLM
```

扩展 100+ Provider。

## 启发

这是很值得参考的：

> “同一个用户，不同任务，使用不同 Provider Profile”。

---

# 17. Continue

Continue 的特点：

```text
模型分角色
```

例如：

```text
chat model
edit model
autocomplete model
embed model
rerank model
```

## 启发

这是未来趋势：

> 不再是“一个模型干所有事”。

而是：

```text
不同能力 → 不同模型
```

---

# 18. Zed

Zed 的特点：

```text
大量 Provider
本地模型
BYOK
```

支持：

```text
OpenAI
Anthropic
DeepSeek
OpenRouter
Ollama
LM Studio
Bedrock
Vercel AI Gateway
```

## 启发

成熟产品会支持：

```text
Official Provider
OpenAI-compatible
Local Model
Gateway
```

四类入口。

---

# 19. OpenCode

OpenCode 的特点：

```text
AI SDK + Models.dev
```

实现：

```text
75+ Provider
```

## 启发

Provider 广覆盖：

```text
可以借助 SDK / Gateway
```

但：

```text
深度体验
仍然要自己做
```

---

# 20. Cursor / Windsurf

商业 Coding Agent：

```text
体验优先
```

特点：

```text
Provider 不完全开放
BYOK 有限制
```

原因：

```text
成本控制
体验一致性
产品治理
```

## 启发

商业产品：

```text
通常会牺牲一部分自由度
换体验
```

---

# 21. LiteLLM / OpenRouter / AI Gateway 已成行业基础设施

这是一个非常重要的行业趋势。

很多产品：

```text
并不直接集成所有 Provider
```

而是：

```text
LiteLLM
OpenRouter
Vercel AI Gateway
```

做：

```text
fallback
routing
统一 API
quota
usage
high availability
```

## 但它们的局限

它们主要统一：

```text
Chat
Completion
Tool Calling
Routing
```

不会替产品统一：

```text
Files API
Knowledge Base
Citation
Page Reference
File Lifecycle
Provider File Cleanup
```

所以：

> 文件层和 RAG 层依然应该属于产品。

---

# 22. 行业已经普遍做好的能力

主流 Agent 应用基本都会做：

```text
1. 多 Provider
2. BYOK
3. OpenAI-compatible
4. Streaming
5. Tool Calling
6. 文件上传
7. 自建 RAG
8. 向量数据库
9. Workflow
10. Agent
11. MCP / Plugin
12. 本地模型
13. API Key 校验
```

这些已经逐渐变成：

```text
基础能力
```

---

# 23. 行业普遍还没做好的能力

这里才是真正的机会。

---

## 23.1 Provider 原生文件能力没有统一打穿

大多数产品：

```text
支持文件上传
```

但：

```text
不是 Provider 原生文件 API
```

而是：

```text
自己解析
自己 chunk
自己 embedding
自己 retrieval
```

很少真正深度利用：

```text
OpenAI Responses input_file
OpenAI File Search
Gemini File API
Claude document block
Qwen-Long
Kimi file extraction
火山/百度/腾讯/智谱原生知识库
```

---

## 23.2 Capability Detection 很粗糙

大多数产品只做到：

```text
provider -> model list
```

而没有做到：

```text
provider + model + task + file type + strategy
```

行业缺少：

```text
精细 capability schema
```

---

## 23.3 Citation / 页码统一很弱

很多产品：

```text
引用不稳定
页码错误
表格引用差
扫描件支持差
```

这里有很大优化空间。

---

## 23.4 Usage / Quota / Cost 治理不够

BYOK 产品经常遇到：

```text
余额不足
权限不足
region 不支持
文件额外收费
不同 Provider usage 格式不同
```

大多数产品：

```text
只是简单报错
```

而不是：

```text
统一 usage / quota / routing / fallback
```

---

## 23.5 Tool Calling 差异没有真正被抽象

不同 Provider：

```text
OpenAI tools
Anthropic tool_use
Gemini function calling
Mistral tools
Qwen tools
```

差异很大。

多数产品：

```text
只是浅层兼容
```

复杂 Agent：

```text
仍会出现：
tool 参数错误
parallel tools 差异
streaming 差异
```

---

# 24. 对你们产品的直接启发

你们不应该只做：

```text
支持更多 Provider
```

因为：

```text
OpenAI-compatible + LiteLLM
已经能解决很多广覆盖问题
```

真正有价值的是：

```text
统一复杂性
```

---

# 25. 推荐的产品路线

---

## 第一层：Generic Provider

覆盖长尾。

支持：

```text
OpenAI-compatible
LiteLLM
OpenRouter
Vercel AI Gateway
Local Models
```

只承诺：

```text
文本 Chat
Streaming
基础 Tool Calling
RAG 后文本输入
```

---

## 第二层：Tier 1 深度集成

重点做：

```text
OpenAI
Gemini
Claude
Qwen
DeepSeek
Kimi
```

其中：

```text
OpenAI / Gemini / Claude / Qwen：
原生文件 + 自建 RAG 双路径

DeepSeek：
自建解析 + RAG + 文本输入

Kimi：
文件抽取 + 长上下文
```

---

## 第三层：文件层属于产品

建议产品自己实现：

```text
File Service
Parser
OCR
Chunker
Embedding
RAG
Citation
File Strategy Planner
```

Provider 原生文件 API：

```text
只是 fast path
```

---

## 第四层：Capability Registry

行业普遍缺：

```text
细粒度 capability schema
```

你们可以做：

```ts
type ProviderCapability = {
  directFileInput: boolean;
  providerFileSearch: boolean;
  providerFileExtraction: boolean;

  supportsPdf: boolean;
  supportsDocx: boolean;
  supportsScannedPdf: boolean;

  supportsFileUrl: boolean;
  supportsBase64File: boolean;
  supportsProviderFileId: boolean;

  supportsCitation: boolean;
  supportsPageReference: boolean;
};
```

---

# 26. 最终结论

现在主流 Agent 应用已经做到：

```text
多 Provider
BYOK
OpenAI-compatible
文件上传
RAG
Workflow
Tool Calling
MCP
本地模型
```

但普遍没有做到：

```text
Provider 原生文件能力统一
Capability 精细抽象
Citation 统一
Usage / Quota 治理
文件策略自动路由
Tool Calling 深度兼容
```

因此：

> 真正的机会，不是“支持更多模型”。

而是：

```text
把用户自带 LLM API 的复杂性藏起来
```

通过：

```text
统一文件层
统一 RAG
统一 Citation
统一 Capability
统一 Planner
统一 Error
统一 Usage
```

构建：

> BYOK LLM Orchestration + File Intelligence Layer
